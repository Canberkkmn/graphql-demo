# graphql-demo

This is a simple demo of a GraphQL server using Apollo Server and Express.

## Installation

1. Clone the repository
2. Run `npm install`
3. Run `node server.js`

## Usage

The server will be running on `http://localhost:4000/`. You can use the GraphQL playground to interact with the server.
